<h1>New Review</h1>

<%= simple_form_for [@restaurant, @review] fo |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: (0..5).to_a %>
  <%= f.button :submit, "Submit Review", class: "btn btn-primary" %>
<% end %>
